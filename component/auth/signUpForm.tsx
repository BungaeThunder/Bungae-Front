import React from 'react';

type ISignUpFormProps = {
	name: string;
	password: string;
};

export const signUpForm: React.FC<ISignUpFormProps> = ({ name, password }) => {
	return (
		<div>
			<p>111</p>
		</div>
	);
};
